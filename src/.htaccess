<IfModule mod_rewrite.c>
  RewriteEngine On RewriteBase /outside/ RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d RewriteCond %{REQUEST_FILENAME} !-l
  RewriteRule . /outside/index.html [L]
</IfModule>
